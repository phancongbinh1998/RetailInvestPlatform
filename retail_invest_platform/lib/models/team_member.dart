class TeamMember{
  int id;
  String name;
  String position;
  String description;
  String avatarImageUrl;
  int projectId;

  TeamMember(this.id, this.name, this.position, this.description,
      this.avatarImageUrl, this.projectId);
}